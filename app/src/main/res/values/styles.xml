<resources>

    <style name="Evan"/>
    <!-- 统一的字体 -->
    <style name="Evan.TextAppearance"/>

    <style name="Evan.TextAppearance.Large">
        <item name="android:textSize">@dimen/text_size_large</item>
    </style>

    <style name="Evan.TextAppearance.Large.Blue">
        <item name="android:textColor">@color/blue</item>
    </style>

    <style name="Evan.TextAppearance.Small">
        <item name="android:textSize">@dimen/text_size_small</item>
    </style>

    <style name="Evan.TextAppearance.Small.Gray">
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="Evan.ListView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:cacheColorHint">@android:color/transparent</item>
        <item name="android:divider">#ffdedede</item>
        <item name="android:dividerHeight">1px</item>
        <item name="android:drawSelectorOnTop">false</item>
        <item name="android:fadingEdge">none</item>
    </style>

    <style name="Evan.Theme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Evan.Theme.Translucent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
</resources>
